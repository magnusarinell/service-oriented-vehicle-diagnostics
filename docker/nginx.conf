server {
    listen 80;

    location /api/ {
        proxy_pass         http://sovd-server:8080;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
    }

    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri $uri/ /index.html;
    }
}
